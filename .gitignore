# ============================================================
# 懒懒衣橱（wardrobe-miniapp）—— 微信小程序 + 云开发
# ============================================================

# ---------- 云函数环境变量：绝不入库 ----------
# ⚠️ 本项目的 5 组凭证（SILICONFLOW_KEY / WANX_KEY / COS_SECRET_ID /
#    COS_SECRET_KEY / AMAP_KEY）一律配在**云开发控制台的环境变量**里，
#    不以文件形式存在、也绝不允许进仓库。
#    下面几条是兜底：万一哪天有人把密钥导成本地文件，别让它被 add 进来。
.env
.env.*
*.env
**/config.local.js
**/*.secret.json

# ---------- 微信开发者工具 ----------
# 本地私有配置：工具自动生成、因人而异（个人设置 / 本地路径 / 编译模式）
project.private.config.json
# 工具产物
miniprogram_npm/
.idea/
.vscode/

# ---------- 依赖 ----------
node_modules/
**/node_modules/

# ---------- 系统 / 编辑器 ----------
.DS_Store
Thumbs.db
*.swp
*~

# ---------- 日志 ----------
*.log
npm-debug.log*
yarn-error.log*

# ============================================================
# 公开副本额外排除（方法论仓：仅跟踪方法论层，业务大脑/画像不公开）
# ============================================================
# 业务大脑：prompt 体系、案例库、个人画像 —— 属「业务设计」，按公开边界保留不公开
cloudfunctions/
pages/
utils/
dev/genLookImage/
app.js
app.wxss

# 顶级 dev 测试/运行器依赖被排除的私有模块（clone 下来必 broken import）
# → 仅保留自包含、零依赖的 dev/demo-gates/（node dev/demo-gates/run.js 即跑）
dev/run-gates.js
dev/*.test.js
# 反向验证脚本内嵌了私有页面/工具的真实代码片段（pages/recommend、utils/lookDiy）→ 不公开
dev/shortcut-check-async-frontend.js
images/
seed/
project.config.json
.scratch/
.workbuddy/
wardrobe-miniapp.lnk
列出所有含

# 生成物 / 日志 / 本地
outputs/
generated-images/
.rp.log
.vl.log
*.local.json

# 内部工作文档（不对外）
docs/specs/
docs/tickets/
docs/公开方案-可执行.md
