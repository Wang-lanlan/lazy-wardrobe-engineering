# 我的衣橱 · 微信小程序设计定稿（v2）

grill 日期：2026-08-30 ｜ 状态：**已作废，仅作历史留档**

> ## ⚠️ 本文件已过期，不要再作为依据
>
> 设计阶段的部分结论在实现过程中被修正，以下内容**与线上代码不一致**：
>
> | 项目 | DESIGN.md（旧） | 实际（现） |
> |---|---|---|
> | 生图模型 | `HY-Image-v3.0-I2I` | 有参考照 → `Qwen/Qwen-Image-Edit-2509`；无参考照 → `Tongyi-MAI/Z-Image-Turbo` |
> | 生图单价 | ¥0.10/张 | ¥0.036–0.10/张（按有无参考照分档） |
> | 组合缓存键 | 未明确 | = 单品集合 **+ 场景** |
> | 档案入口 | 衣橱页右上角按钮 | 底部第 4 个 tab |
>
> **决策理由请查 `docs/adr/`（0001–0006）；搭建与运维请查 `docs/SETUP.md`；术语定义请查 `CONTEXT.md`。**

---

## 一、已锁定的决策

| # | 决策点 | 结论 | 理由 |
|---|---|---|---|
| 1 | 功能范围 | 衣橱管理 + AI 穿搭推荐 | 解决「随时能看能改」+「今天穿什么」 |
| 2 | AI 运行位置 | 云函数直连大模型 | 真·实时，费用按 token，自用极低 |
| 3 | 天气来源 | 自动定位 + 高德 API | 推荐准确度优先 |
| 4 | 推荐交互 | 参数（场景）+ 一句话补充 → 出 3 套 | 省 token、结果可控、可「换一换」 |
| 5 | 方案默认配图 | 衣橱现有单品图拼贴 | 秒出、0 成本 |
| 6 | 图片迁移 | 只迁清单，图留空，按需补 | 要录真实衣服，不用 AI 示意图占位 |
| 7 | 数据主权 | 小程序云为唯一真源，本地版退休 | 终结「网页改了、技能没同步」的分叉 |
| 8 | 上线方式 | 体验版自用，不提审 | 绕开个人主体 AI 类目审核风险 |
| 9 | 方案历史 | 做收藏 + 历史页（可打标签） | 攒出经过验证的搭配库 |
| 10 | 每日推送 | 不做 | 订阅消息一次授权一条，体验别扭 |
| 11 | 上身图人像来源 | 用户上传一张参考照 → 图生图 | 真「你穿上这套」，比模特图直观得多 |
| 12 | 上身图触发方式 | 默认拼贴，点「看上身效果」才生成 | 已确认：credits 路径也是「换个钱包付钱」且要跨应用手工搬运，即时方案体验完胜 |
| 13 | 生图链路 | 硅基流动 Z-Image-Turbo（¥0.10/张） | 五条链路比价后最划算：约 3 元/月，国产模型、出图快、中文友好、商用授权、支持图生图 |
| 14 | 生图层架构 | 可插拔 provider（配置项切换） | 不满意可随时换模型，不用重写代码，试错成本近零 |

### 体验设计：让成本不成为负担

按需生图已经把次数压下来了，但「每次点击都在花钱」的心理负担才是体验杀手。三条硬规则：

1. **绝不弹按次扣费确认**——点「看上身效果」立刻开始生成，不打断。那种「本次将扣费 0.5 元」的弹窗是焦虑的来源，会让你不敢点，功能等于废掉
2. **额度条而非弹窗**——设置页常驻显示「本月 6 / 30 张 · 约 3 元」，成本可见、可控，但不在操作路径上拦你
3. **缓存命中要明确告知**——同一套搭配再点，秒开并提示「已生成过，本次不扣费」，让「复用不花钱」这件事被感知到

补充：生成完的图可一键存相册；未上传参考照时自动降级为「按身形参数文生图」（模特图，效果弱一档但可用）。

**除此之外我直接拍板的**
- 大模型：**DeepSeek**（云开发 AI+ 内置，中文好、最便宜）
- 天气 provider：**高德开放平台**（免费额度大、注册即用、单 key）
- 生图模型：**HY-Image-v3.0-I2I**（图生图，支持 1 张参考图 / ≤10MB / jpg·png）
- 技术栈：**原生小程序**（不用 uni-app/Taro，减少依赖和体积）
- 云函数超时：默认 3s → **60s**
- 成本防护：① 相同单品组合的图永久缓存 ② 每日生成上限，超了提示

---

## 二、架构

```
微信小程序（原生）
   │
   ├─ 云数据库 CloudBase
   │    ├─ items      单品（沿用 wardrobe.json 字段 + cloudFileID）
   │    ├─ meta       穿搭档案 + 设置 + 参考照 fileID
   │    ├─ looks      收藏的方案
   │    └─ genCache   已生成上身图索引（key = 单品 id 组合 hash）
   │
   ├─ 云存储
   │    ├─ wardrobe/   单品照片
   │    ├─ reference/  你的参考照（仅 1 张）
   │    └─ looks/      生成的 AI 上身图（按组合缓存复用）
   │
   └─ 云函数
        ├─ aiRecommend  衣橱摘要 + 天气 + 场景 + 备注 → DeepSeek → 3 套结构化方案
        ├─ genLookImage 生图（可插拔 provider）
        │               默认：硅基流动 Z-Image-Turbo（图生图，¥0.10/张）
        │               备选：通义万相 ¥0.2｜混元官方 ¥0.5｜阿里云百炼「演绎」qwen-image-3.0 ¥0.20
        │               输入：参考照 fileID + 该套单品描述 → 上身图，写缓存后返回
        ├─ getWeather   高德 API，按「城市+小时」缓存
        ├─ seedImport   一次性导入 54 条清单（跑完即废弃）
        └─ exportData   导出 JSON（供本地 ootd 技能出方案时拉取）
```

**权限**：`scope.userLocation`（定位拿城市）、`chooseMedia`（相机/相册）

**大模型输入**：衣橱摘要（类别+颜色+版型+材质+季节+场景，54 件约 3-4k token）+ 天气 + 场景 + 用户备注 + 穿搭档案（身形/肤色/雷区/想多穿）+ 最近收藏方案（去重）
**输出**：严格 JSON，3 套，每套含单品 id 列表 + 搭配逻辑 + 一句点睛

**图生图输入**：你的参考照 fileID + 该套单品的文字描述（颜色/版型/材质/穿法）+ 场景氛围词
**输出**：一张竖版上身效果图，存云存储并写入 genCache，同一组合二次调用直接读缓存

---

## 三、页面结构

**Tab 1 · 衣橱**
- 顶部：类别 chips + 季节 + 场景 + 搜索
- 卡片网格：有图显示图；**无图显示色块 + 「拍照」按钮**
- ＋ 添加单品 ｜ 详情：编辑 / 删除 / 状态切换（在用 ⇄ 收纳）
- 右上角：穿搭档案（身高体重、身形、肤色、雷区、想多穿、**参考照管理**）

**Tab 2 · 推荐**
- 场景 chips（上班/休闲/约会/旅行）+ 自动天气（可手改）+ 备注输入
- 生成 → 3 套卡片：每套 = 单品图横向拼贴 + 搭配逻辑 + 「**看上身效果**」「收藏」「换一换」
- 点「看上身效果」→ 转圈 10-20 秒 → 出图，可保存到相册

**Tab 3 · 收藏**
- 历史方案列表，标签筛选（见客户 / 约会专用…）
- 点开看这套用了哪几件，可在衣橱里定位；已生成过的上身图直接显示（缓存命中，0 成本）

---

## 四、费用预估

| 项 | 单价 | 预估月费 |
|---|---|---|
| 云开发 | 免费档 3000 资源点/月，超出 19.9 元/月 | 0 ～ 19.9 元 |
| 大模型（DeepSeek） | 按 token | 约 0.5 – 2 元 |
| 天气（高德） | 免费额度 | 0 元 |
| 上身图（Z-Image-Turbo） | 0.10 元/张，按需生成 | 约 1 – 3 元 |
| **合计** | | **约 2 – 25 元/月** |

按「每天生成一次、只对中意的一套点看上身效果」估算：0.10 元/天 ≈ **3 元/月**。完全不看上身图则 0 元。

> 成本下降关键：生图链路从混元官方（¥0.5/张）换到 Z-Image-Turbo（¥0.10/张），**便宜 5 倍**；叠加按需触发 + 组合缓存，实际月支出比一顿早餐还低。

---

## 五、分工

**我来做**
1. 小程序全部代码（3 个 tab + 5 个云函数 + 数据库设计）
2. 数据迁移脚本：wardrobe.json 的 54 条清单 → 云数据库
3. 部署图文指引 + 本地 ootd 技能改造（改为从云拉取数据）

**你需要做（约 40 分钟）**
1. mp.weixin.qq.com 注册小程序账号（个人主体，类目「工具 > 效率」）→ 拿 **AppID**
2. 下载安装**微信开发者工具**
3. 注册**高德开放平台** → 创建应用 → 拿 **Web 服务 key**
4. 注册**硅基流动 SiliconFlow** → 拿 **API key**（生图用，¥0.10/张，按需扣费无套餐）
5. 准备**一张参考照**：正面/大半身、光线均匀、穿贴身基础款（背心+legging 最佳）、背景干净
6. 把三个 key 给我（AppID / 高德 key / 硅基流动 key）
7. 微信开发者工具开通**云开发**（免费档）→ 导入项目 → 上传 → 扫体验版二维码，完成
